export type Category = {
    _id: string;
    name: string;
    description?: string; // Optional field
};

export type ProjectCategory = {
    _id: string;
    name: string;
    description?: string; // Optional field
};
